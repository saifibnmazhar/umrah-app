You are working inside a Laravel project.

There is an existing tasklist file located at:

# Laravel UI Integration – OpenCode Commands.txt

This file contains a Laravel UI integration tasklist, but it is INCOMPLETE because it was originally generated without full context of the UI reference codebase.

You also have full access to the UI reference folder:

./ui-references/

This folder contains the original HTML, JS, and assets used to build the frontend.

---

YOUR TASK:

You MUST update and enhance the existing tasklist file:

# Laravel UI Integration – OpenCode Commands.txt

---

STEP 1: READ FULL CONTEXT
- Read all files inside ./ui-references/
- Understand:
  - page structure
  - layout structure
  - repeated UI sections
  - JavaScript behaviors
  - form structures
  - reusable UI patterns

---

STEP 2: OPEN AND ANALYZE EXISTING TASKLIST FILE
File:
# Laravel UI Integration – OpenCode Commands.txt

- Understand current tasks
- Identify missing parts
- Identify generic or incomplete instructions

---

STEP 3: UPDATE TASKLIST (DO NOT REWRITE BLINDLY)

You MUST:
- Keep existing structure where useful
- Enhance it using real UI reference codebase knowledge
- Replace generic tasks with concrete implementation steps
- Add missing tasks discovered from UI reference
- Remove redundant or incorrect instructions

---

STEP 4: CONVERT ALL TASKS INTO EXECUTABLE COMMAND FORMAT

Each task MUST include:
- Exact Laravel file paths
- Exact Blade view names
- Exact routes (routes/web.php entries)
- Exact component names
- Exact partial extraction points from UI reference
- Exact Alpine.js conversions where JS exists
- Dummy data structures based on UI forms/tables

NO GENERIC PLACEHOLDERS ARE ALLOWED.

---

STEP 5: ALIGN WITH REAL UI STRUCTURE

From ./ui-references/, infer and map:
- HTML pages → Blade views
- Layout → app.blade.php
- Navbar/sidebar/footer → partials
- JS interactions → Alpine.js components
- Repeated UI blocks → Blade components
- Tables/forms → dummy dataset structures

---

STEP 6: OUTPUT FORMAT (STRICT)

You MUST update the file:

# Laravel UI Integration – OpenCode Commands.txt

The final output must be:
- Fully updated tasklist
- Production-ready
- Execution-level instructions
- Based on real codebase analysis
- No vague Laravel explanations
- No placeholders like "example page"

---

GOAL:
Turn the existing incomplete tasklist into a fully detailed, codebase-aware execution plan for Laravel UI integration.